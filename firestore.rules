rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read access for portfolio data
      allow read: if true;
      // Keep write restricted for security
      allow write: if false;
    }
  }
}
